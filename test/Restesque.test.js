var chai = require("chai");
var expect = chai.expect;

var Restesque = require('./../src/Restesque');


describe('Restesque', function () {
    describe('Restesque', function () {
        it('can be initialised', function () {
            var restesque = new Restesque();
            expect(restesque).to.be.ok;
        });
    });
});

